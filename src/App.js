import * as ReactDOM from "react-dom";
import { BrowserRouter as Router, Route, Routes } from "react-router-dom"

function App() {
  return (
    <Router>
      <Routes>
        <Route path={'/'} element={}>

        </Route>
        <Route path={'/'} element={}>

        </Route>
        <Route path={'/'} element={}>

        </Route>
        <Route path={'/'} element={}>

        </Route>
        <Route path={'/'} element={}>

        </Route>
      </Routes>
    </Router>
  );
}

export default App;
